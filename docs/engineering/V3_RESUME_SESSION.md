# V3 RESUME SESSION — موجّه الاستئناف الرفيق للدستور V3
> يُستعمل حرفيًا لبدء أي جلسة تعمل تحت MASTER ENGINEERING CONSTITUTION V3.
> الحالة: **PROPOSED مع V3** — لا يُفعَّل إلا بعد تبنّي المالك للدستور V3.

---

أنت تستأنف العمل على مشروع **WebDev AI Editor / editor_v4** تحت حكم
**MASTER ENGINEERING CONSTITUTION V3** (`docs/engineering/CONSTITUTION_V3.md`).

## الخطوة 0 — المراسي السلوكية (قبل أي فعل)
اقرأ V3 §0 وطبّقه: الدليل فوق الذاكرة · UNKNOWN فوق الاختراع · append-only ·
لا push/branch/tag/PR أبدًا · الشجرة منشورة دائمًا (لا أسرار فيها) · لا اعتماد
يُكتب في أي ملف · الاستشهاد الجديد مؤرَّخ `file:line @ <commit|date>` ·
استئناف لا إعادة تشغيل.

## الخطوة 1 — البيئة
إن كانت بيئة العمل مفقودة (sandbox مُسح): نفّذ V3 §3.1 كاملًا —
استنساخ (بترشيح الإخراج) → تطهير remote فورًا → هوية git → تحقق HEAD/الشجرة →
تدقيق ما-بعد-الاستنساخ §3.3 (قيود غير معروفة + ملفات دخيلة).
إن لُصق اعتماد في نص التكليف: طبّق V3 §3.2 (استنساخ فقط، صفر كتابة،
وذكّر المالك بحذفه/تدويره في نهاية الجلسة).

## الخطوة 2 — الموضع
اقرأ `docs/engineering/PROGRESS.md` كاملًا — هو السلطة الوحيدة للحالة.

**خريطة الموضع عند أول جلسة V3** (كما ثبّتها GENESIS @ 03c7eab):
- المشروع في **STAGE 4 — CLOSED-AWAITING-OWNER-DIRECTION** (V3 §8).
- **المقفل**: برنامج V1 كاملًا — Stage 3 = 26/26 TSK؛ M6–M10؛ IR-1 + IR-2؛
  RRR §5 = GO (عقد localhost أحادي المستخدم)؛ خط الأساس 1901 = 0F/1867P/34S؛
  check.sh ALL GREEN exit 0؛ صفر دين (غياب TECHNICAL_DEBT.md معلَّل).
- **الـbacklog المفتوح** (بموجب IR-2 — اختياري، ليس دينًا):
  FI-01 توحيد حالة REST/WS · FI-03 إيقاف رشيق · FI-04 مقعد redis/worker ·
  FI-05 استمرارية الفهرس · FI-06 سجلات مهيكلة · FI-07 تفكيك app.js ·
  FI-09 تمرير افتراضي · FI-10 DOMPurify · FI-11 مواصفة إطارات WS ·
  FI-12 دليل نشر/تهديدات · + CP-4 hooks (ADOPT-CANDIDATE مؤجل بقرار IR-1).
- **القرار المنتظر**: اختيار المالك للدفعة التالية (أو توجيه جديد).

## الخطوة 3 — نوع الجلسة
- **إن كانت الجلسة الأولى بعد إقفال، أو تخطيط دفعة جديدة، أو بأمر مالك**:
  هذه جلسة **كبرى** → نفّذ اكتشاف الحالة المضغوط V3 §7.2 (جرد → فجوات → اتساق →
  فحص تقادم as-of) **قبل** أي تخطيط. اعرض على المالك الـbacklog أعلاه مع
  كلفة/عائد كل بند (من FUTURE_IMPROVEMENTS.md) واطلب قراره.
- **إن كان برنامج جارٍ بالفعل** (PROGRESS.md يظهر Stage 1–3 نشطة):
  جلسة **عادية** → V3 §7.1: استأنف من Current Position حرفيًا.

## الخطوة 4 — أثناء العمل
- كل مهمة بدورة TSK الكاملة (V3 §9.2) وبواباتها (§9.3) — لا تُضعَف بوابة أبدًا.
- كل تغيير معماري: ADR + DECISION_LOG **قبل** الكود.
- قيد محلي مبكر بعد كل مخرج مرحلي (V3 §3.4).
- PROGRESS.md يُحدَّث قبل نهاية الجلسة (موضع + سجل جلسة append-only).

## الخطوة 5 — نهاية الجلسة
1) حدّث PROGRESS.md (وطبّق تدوير §6.4 إن تجاوز السجل 400 سطر).
2) قيود محلية لكل عمل غير مقيَّد. لا push إلا بأمر مالك.
3) **ذكّر المالك بحذف/تدوير أي اعتماد لُصق في هذه الجلسة** (V3 §3.2 — إلزامي).

## تذكيرات معلّقة للمالك (من GENESIS — تُعرض في أول جلسة V3)
- تبنّي V3 رسميًا (وعندها فقط: إلحاق سطر `[SUPERSEDED by V3 — <تاريخ>]` أعلى `prompet_28_7_final.md`).
- G-11: تقادم وسوم VERIFIED في `PRODUCT_VISION.md` (مثبتة على عصر d4b8562) — مُشغِّل تعديل بإجراء §16 الخاص بها.
- G-10: مصير الفصول صفرية-البايت في `docs/engineering_constitution/` (إبقاء موسومًا أو سحب).
- تدوير كل التوكنات الملصقة في جلسات GENESIS (≥2 توكن مكشوف).
