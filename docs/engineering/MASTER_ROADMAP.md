# MASTER_ROADMAP.md — editor_v4 (P4 — CORE-ONLY SCOPE v4.1)

> الحالة تُدار في PROGRESS.md فقط. النطاق SECTION 0.8 — لا milestone يمس Provider Layer.
> المدخلات: مجموعة Confirmed من P2 {BUG-01, BUG-03(آلية), BUG-04, A3} +
> صفوف NF المطلوب لها TSK من P3 (الجدول التجميعي في NEW_FINDINGS.md).
> المهام الذرية نفسها تُفصَّل في P5 (IMPLEMENTATION_TASKS.md) — هنا التجميع والترتيب فقط.

---

## مبادئ الترتيب

1. **M1 RULE**: أول milestone = إصلاحات السلامة المؤكدة الأعلى شدة فقط
   (كل S2 المؤكد C3+)، بلا أي refactor غير ضروري لها.
2. التكتّل حول **سبب جذري مشترك** لا حول ملف مشترك (مثال: BUG-04 + التكرارات
   الثلاثة تُحل بقرار واحد — قائمة تجاهل موحّدة).
3. لا milestone يعتمد على milestone لاحق (DAG صاعد فقط).
4. كل milestone قابل للشحن مستقلًا وخلفه QA-T مخصص (P6).

---

## M1 — Safety First: إغلاق الـ S2 المؤكدة (يُنفَّذ أولًا وحصريًا)

**تبرير M1 RULE من مخرجات P2/P3 الفعلية**: المجموعة المؤكدة بأعلى شدة (S2) هي
بالضبط: BUG-01 (C4) + NF-13 الملتصق به سببيًا (نفس الـ fallback)، BUG-03-آلية
(C4)، NF-15 Zip-Slip (C4). كلها مخاطر «كتابة/تنفيذ/تسريب غير مقصود» تمس
المستخدم مباشرة، وكلها قابلة للإصلاح موضعيًا بلا إعادة هيكلة — لذا تتقدم على
أي شيء آخر.

| حقل | قيمة |
|---|---|
| Scope items | BUG-01 + NF-13 (تمرير mode للمحلّل/فلترة actions في chat + تهذيب fallback الأوامر)؛ BUG-03 (توحيد مسارات الحقن L1332–1339/L1782–1791/history تحت ContextBudget)؛ NF-15 (فحص أعضاء ZIP قبل extractall) |
| Entry criteria | P5 tasks معتمدة؛ goldens الحالية خضراء |
| Exit criteria | QA-T: رد chat بأمثلة كود لا يولّد actions؛ حمولة سياق مسقوفة بميزانية واحدة قابلة للقياس؛ ZIP خبيث يُرفض | 
| Risk | متوسط — يلمس مسارَي الإرسال المكررين (L1862–1925) → يعتمد جزئيًا على قرار M2-توحيد أو ازدواج مؤقت موثَّق |
| Out-of-scope guard | لا مساس بأي فرع provider-selection |

## M2 — Single Source Consolidation: فكّ التكرارات الحاملة للأعطال

**لماذا ثانيًا**: NF-23(4) هو الآلية البنيوية لـ BUG-04؛ وتوحيد الكتل المكررة
يخفض كلفة كل إصلاح لاحق للنصف.

| حقل | قيمة |
|---|---|
| Scope items | BUG-04 عبر NF-23(4): قائمة تجاهل واحدة (تضم `test-results` و`test---results`) تُستهلك في file_manager + bridge + agent_tools؛ NF-23(1) دمج apply_all_actions/execute_plan؛ NF-23(2) تعريف MAX_SMART_FILE_SIZE الواحد؛ NF-23(3) قارئ config واحد |
| Entry criteria | M1 مشحون (أو متوازٍ بعد تجميد L1862–1925) |
| Exit criteria | QA-T يعيد سيناريو T02: فخ ورقة الإجابة في كلا المجلدين غير مقروء من المسارات الثلاثة |
| Risk | منخفض — استخلاص ثوابت ودوال مشتركة |

## M3 — Runtime Robustness: الخيوط والذاكرة والأخطاء

| حقل | قيمة |
|---|---|
| Scope items | NF-01 (تنظيف pending داخل القفل)؛ NF-04 (تخييط apply الطويل أو checkpoints إلغاء)؛ NF-06 (طَهْر تذاكر terminal)؛ NF-07 (سقف تاريخ — يُنسَّق مع ميزانية M1)؛ NF-14 (تضييق except + log موحّد للمواضع الحرجة)؛ NF-02 (تمرير project_id الفعلي أو توثيق سياسة الخانة الواحدة) |
| Exit criteria | اختبارات وحدات للسباق (تنظيف/pop متزامن)؛ جلسة 500 رسالة بلا نمو تذاكر؛ إلغاء يستجيب أثناء apply |
| Risk | متوسط — يلمس invariants السجل (T-014/T-015) — عقود الاختبارات القائمة (contracts/) هي الحارس |

## M4 — Frontend & Streaming UX

| حقل | قيمة |
|---|---|
| Scope items | NF-10 (append تدريجي/throttle بدل إعادة render كاملة)؛ NF-11 (backoff+jitter+try/catch onmessage)؛ NF-12/A3 (إطار scan_start + مؤشر «جاري التفكير» — الطلب التاريخي للمستخدم)؛ NF-18 (تسييج المحتوى المحقون بعلامات حدود في البرومبت) |
| Exit criteria | رد 100KB يُبث بلا تجمّد قابل للقياس؛ سقوط الخادم لا يولّد إعادة اتصال عاصفة؛ إشارة مرئية ≤200ms من الإرسال |
| Risk | منخفض — frontend معزول + قالب برومبت |

## M5 — Performance & Search

| حقل | قيمة |
|---|---|
| Scope items | NF-20 + NF-21 (فهرس بحث مشترك يقوم على ProjectIndex القائم أصلًا بخطافات write-through — يخدم api_search وtool_search_code معًا)؛ NF-16 (توثيق حدود النشر localhost + راية إلزام approval) |
| Exit criteria | بحث في مستودع 5k ملف < 1s؛ AgentLoop لا يعيد قراءة الشجرة كاملة كل نداء |
| Risk | متوسط — تصميم فهرس؛ يُبنى فوق ProjectIndex فلا بنية جديدة |

---

## خارطة التبعيات (DAG)

```
M1 ──► M3 (تنسيق سقف التاريخ مع الميزانية)
M1 ⇄ M2 (تجميد الكتلتين المكررتين إن توازيا — قرار P5)
M2 ──► M5 (قائمة التجاهل الموحدة يستهلكها الفهرس)
M4 مستقل (يبدأ بعد M1 لأولوية الموارد فقط)
```
لا دورات — يحقق مبدأ (3).

## عناصر بلا milestone (مسجَّلة، لا تنفيذ الآن)

| عنصر | وجهة |
|---|---|
| NF-03 ازدواجية REST/WS | قرار معماري → P7 (توحيد على SessionContext أو إسقاط REST المتحولة) |
| NF-05 خيوط بلا join | P7 |
| NF-08 TTL مشروط بالنشاط | ملاحظة فقط |
| NF-17 / A6 | إغلاق عبر QA-T في P6 (لا كود) |
| NF-19, NF-24 | إيجابيات — حماية بـ QA-T انحداري فقط |

## DoD (P4c)

- [x] كل milestone بحقول كاملة (scope/entry/exit/risk) — أعلاه.
- [x] M1 RULE مبرَّر من مخرجات P2/P3 الفعلية (مجموعة S2 المؤكدة حصريًا).
- [x] لا عنصر يمس providers/ أو fallback أو routing (فحص يدوي لكل صف).
- [x] كل BUG/NF المطلوب له TSK موزَّع على milestone أو جدول «بلا milestone».
- التتبّع الثنائي الاتجاه يكتمل في P5 (TSK ⇄ BUG/NF ⇄ QA-T).
