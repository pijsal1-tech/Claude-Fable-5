# 📋 سجل الأخطاء والمشاكل (BUG_REPORT.md)
> **المشروع:** `editor.._v5_3`
> **نوع التقرير:** مشاكل فقط بدون مميزات (Defects & Issues Only)
> **الهدف:** مرجع للمهندسين للإصلاح لاحقاً

---

## 🔴 BUG-001: فشل تشغيل السيرفر الرئيسي `server.py` بسبب ملف مزود مفقود

### 1. وصف المشكلة
عند محاولة تشغيل الخادم الرئيسي `python editor.._v5_3/server.py` ينتهي التشغيل فوراً بخطأ استيراد قاتل (`ModuleNotFoundError`) يمنع تشغيل السيرفر بالكامل.

### 2. المسار والملف المتأثر
- **الملف المتأثر:** [d:\SMS\.hRhRhRhRhRhR\editor.._v5_3\providers\registry.py](file:///d:/SMS/.hRhRhRhRhRhR\editor.._v5_3\providers\registry.py#L9) و `providers/__init__.py`
- **السطر:** 9 (`from .openai_shelby import OpenAIShelbyProvider`)
- **الملف المفقود:** `editor.._v5_3/providers/openai_shelby.py`

### 3. درجة الخطورة والتأثير
- **درجة الخطورة:** 🔴 **Critical (حرج)**
- **التقييم الفني:** `0/10`

---

## 🔴 BUG-002: استيراد مزودات مفقودة في `server.py` يسبب ModuleNotFoundError عند الإقلاع

### 1. وصف المشكلة
الملف الرئيسي `server.py` يقوم باستيراد 5 مزودين غير موجودين نهائياً في مجلد `providers/` مما يسبب إجهاض إقلاع السيرفر عند الوصول للسطور (42-46).

---

## 🔴 BUG-003 / BUG-PATH-003: إظهار كارت اختيار مسار عند ذكر مسار المشروع الحالي في النص

### 1. وصف المشكلة
عند كتابة المسار الحالي للـ Workspace `D:\SMS\1__New_folder` نصياً داخل البرومبت، تقوم الواجهة بإظهار كارت اختيار مسار وخيارات تغيير مجلد العمل بدلاً من معاملتها كتأكيد لنطاق العمل الحقيقي.

---

## 🔴 BUG-FILE-002 / BUG-ACT-002: [CONFIRMED] إنشاء ملف محلي (output.md) في وضع Plan Mode

### 1. وصف المشكلة
عند طلب مراجعة قراءة فقط في وضع الخطة (Plan Mode) والتأكيد على "عدم إنشاء أو تعديل ملفات"؛ قام المحرك بتشغيل `auto_chain (cw_execute)` وإنشاء ملف جديد `output.md` وإظهار نافذة مراجعة التغيرات (`مراجعة التغييرات [قبول الكل / رفض الكل]`)، بينما أدعى الرد النصي كذباً "هل تم تعديل أي ملف؟ لا"!

---

## 🔴 BUG-FILE-003 / BUG-ACT-004: [CONFIRMED CRITICAL] إجبار إنشاء ملف وهمي (script.py) في وضع Edit Mode

### 1. وصف المشكلة
عند طلب مراجعة قراءة فقط صريحة في وضع التعديل (Edit Mode) ومنع التعديل؛ قام المحرك بفرض تشغيل `auto_chain (cw_execute)` وإنشاء وتجهيز ملف بايثون وهمي باسم `script.py` يحتوي على تعليقات و `pass` وفتح نافذة `مراجعة التغييرات` في الواجهة، في حين ادعى الرد النصي في الشات "هل تم تعديل أي ملف؟ لا"!

### 2. الدليل الفعلي التجريبي (Empirical Proof):
- الرد النصي: `هل تم تعديل أي ملف؟ لا`
- شاشة المحرر (اللقطات المرفقة):
  نافذة `مراجعة التغييرات` مفتوحة وبداخلها الملف المبتكر `script.py` مع أسطر مضافة `+3 -0` و `+2 -0` وخيارات `قبول الكل / رفض الكل`.

### 3. درجة الخطورة والتأثير
- **درجة الخطورة:** 🔴 **High / Critical (2/10)**
- **التأثير على المنتج:** عجز وضع Edit Mode عن تنفيذ مهام القراءة فقط بدون فتح نوافذ diff واقتراح ملفات كود وهمية.

---

## 🔴 BUG-MEM-001: تسريب سياق جلسات سابقة (Zombie Context Leak) في رسائل جديدة مستقلة

### 1. وصف المشكلة
عند فتح جلسة محادثة جديدة وإرسال سؤال حسابي بحت `6+6`؛ قام الموديل بحساب النتيجة الصحيحة ولكن تسرب في الرد نص من جلسة سابقة: *"الملف R_rewind.ai/Root/00_CONSTITUTION.md غير موجود في البيئة المتاحة لي..."* بسبب احتفاظ طبقة تجميع السياق (ContextBuilder) بذاكرة متبقية لم تُطهر.

---

## 🔴 BUG-SCOPE-002: انفلات مسح الملفات والبحث في مشاريع خارجية خارج نطاق الـ Workspace

### 1. وصف المشكلة
عند طلب قراءة ملف محدد بمساره `R_rewind.ai/Root/00_CONSTITUTION.md اقراء`؛ قام النظام بتشغيل بحث شامل وتجميع وتحليل ملفات دستورية في مجلدات ومشاريع أخرى خارج نطاق الـ Workspace مثل (`chatx.ai`, `claude.ai.ai`, `Q_qwen.ai`, `R_rewind.ai_V_1`).

---

## 🔴 BUG-ACT-003: تصعيد طلب القراءة البسيطة إلى سلسلة تعديل كاملة (Execute Changes Escalation)

### 1. وصف المشكلة
عند كتابة طلب قراءة فقط "اقراء" لملف محدد؛ قام الـ Smart Router بتصنيف التعقيد بـ `5.5` واختيار `full_chain` والدخول في 7 خطوات استغرقت أكثر من 4 دقائق وانتهت بمرحلة `Execute changes` (تنفيذ تعديلات ملفات)!
