# Development / test dependencies (T-001)
pytest>=8.0
pytest-timeout>=2.3
pytest-cov>=5.0
# TSK-CEV-101 (CEV-F-001): سقف major يمنع انجراف قواعد فحص مستقبلي؛
# stubs مطلوبة للبيئات النظيفة (requests/yaml مستوردة في النطاق المفحوص).
mypy>=1.10,<2
types-requests
types-PyYAML

# SymbolIndex grammars (T-055 / R-205) — تبعية اختيارية:
# غيابها لا يكسر التشغيل (SymbolIndex يتدهور إلى "بلا رموز"
# واختباراته تتخطى نفسها)، لكنها مطلوبة لتغطية goldens في CI.
tree-sitter>=0.25
tree-sitter-python>=0.23
tree-sitter-javascript>=0.23
tree-sitter-typescript>=0.23
tree-sitter-html>=0.23
tree-sitter-css>=0.23

# TSK-729a: توافق درز Redis في كل بوابة محلية (بلا خدمة Redis) —
# fakeredis يحاكي Streams (XADD/XREADGROUP/XAUTOCLAIM)؛ اختياري:
# غيابه ⇒ اختبارات التوافق المزيفة تتخطى نفسها (import-or-skip).
fakeredis>=2.20
