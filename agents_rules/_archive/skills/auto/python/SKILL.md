---
name: python
description: "Python for AI_PR_PR_PROVIDERS. 3 gotchas, 19 conventions, 4 fixes."
domain: python
triggers:
  - glob: "**/*.py"
autoGenerated: true
enabled: true
---

# Python

Auto-compiled from **101 real patterns** in **AI_PR_PR_PROVIDERS**. This skill is auto-routed to agents when working on python files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| ⚠️ GOTCHA: Added JWT tokens authentication — prote | - def waiting(msg: str): +  -     """استنظار ⏳ — مثل step لكن للحالات الجارية (polling/waiting)""" + |
| ⚠️ GOTCHA: Added JWT tokens authentication — prote | -  +         if self.email: -     def _setup(self): +             info(f"MailTm: ✉️  {self.email}")  |
| ⚠️ GOTCHA: Added JWT tokens authentication — prote | -             "User-Agent":   random_ua(), +             "User-Agent":      "Mozilla/5.0 (Windows NT |

## 🔧 Problem Playbooks

### problem-fix in runable_chat.py
File updated (external): .AAA_GGG_iii_VIBE_CODING/Runable/runable_chat.py

Content summary (255 lines):
# -*- coding: utf-8 -*-
"""
Runable Chat Script - بيبعت سؤال لـ Runable ويرجع الرد
Endpoint: POST https://api.runable.com/api/chat/agent
Format: Vercel AI SDK v4 (trigger + message.parts)
Cookies: من accounts_runable.json (آخر حساب)
"""
import sys, io, os, json, uuid, logging

# Fix UTF-8 on Win

**Actionable Steps:**
1. Modified 1 files

### Patched security issue Domains — protects against XSS and CSRF token theft
- # ── Domains المحجوبة من Gensdef load_accounts() -> list:
+ # ── Domains المحجوبة من Genspark ──────────────────────
-     """تحميل الحسابات بأمان — يرجع قائمة فارغة لو الملف مفيش أو corrupt"""
+ _BLOCKED_DOMAINS = {
-     if not ACCOUNTS_FILE.exists():
+     "getnada.com", "dropjar.com", "chapsmail.com",
-         return []
+     "getmule.com", "guysmail.com", "tupmail.com",
-     try:

**Actionable Steps:**
1. Modified 1 files
2. identifier: Domains
3. identifier: Genspark
4. identifier: IndexError
5. identifier: AttributeError

### Patched security issue Module — protects against XSS and CSRF token theft
- # ═══════════════════════════════════════
+ # ════════════════
- # ⚙️ Config — SSOT مرن جداً
+ # ═══════════════════════
- # ═══════════════════════════════════════
+ # ⚙️  Module-Level Constants — Rule 31
- 
+ # ═══════════════════════
- # ── Azure AD B2C ─────────────────────────────────────
+ LOOP_MODE        = True     # وضع التكرار اللانهائي بشكل افتراضي
- B2C_TENANT = "gensparkad

**Actionable Steps:**
1. Modified 1 files
2. identifier: Module
3. identifier: Level
4. identifier: Constants
5. identifier: Rule

### problem-fix in promptcowboy_register.py
-     accounts = load_accounts(cfg.accounts_file) if _SHARED else _load_json(cfg.accounts_file)
+     if _SHARED:
-     accounts = upsert_account(accounts, account) if _SHARED else _upsert(accounts, account)
+         # shared.io.upsert_account بتاخد (filepath, account) وبتحفظ تلقائياً
-     atomic_save(accounts, cfg.accounts_file)
+         upsert_account(cfg.accounts_file, account)
- 
+     else

**Actionable Steps:**
1. Modified 1 files

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Added JWT tokens authentication — protects against XSS and CSRF token theft — confirmed 3x** — - #!/usr/bin/env python3
+ ﻿#!/usr/bin/env python3
- # -*- coding: utf-8 -*-
+ # -*- coding: utf-8 
- 📐 **Added session cookies authentication — confirmed 4x** — -     detected_provider = _guess_provider(mail.email)
+     # ✅ نستخدم chosen_provider مباشرة (مش gu
- 📐 **🟢 Edited P__promptcowboy/promptcowboy_register.py (51 changes, 84min) — confirmed 3x** — Active editing session on P__promptcowboy/promptcowboy_register.py.
51 content changes over 84 minut
- 📐 **Added JWT tokens authentication — protects against XSS and CSRF token theft — confirmed 3x** — -         _load_json,
+         ToolpixMailClient,
-         _save_json,
+         VercelMailClient,
- 📐 **what-changed in promptcowboy_register.py — confirmed 15x** — - DELAY_BETWEEN     = (5, 1)      # ثواني بين كل حساب — رقم واحد أو (min, max) للـ random
+ DELAY_BE
- 📐 **what-changed in promptcowboy_register.py — confirmed 3x** — - DELAY_BETWEEN     = (1, 30)      # ثواني بين كل حساب — رقم واحد أو (min, max) للـ random
+ DELAY_B
- 📐 **Added JWT tokens authentication — protects against XSS and CSRF token theft — confirmed 4x** — -     Factory function — ينشئ email client حسب الـ provider المختار.
+     Factory function — ينشئ e
- 📐 **what-changed in promptcowboy_register.py — confirmed 7x** — - EMAIL_PROVIDER    = "mailtm, toolpix"         # "mailnesia" | "mailtm" | "mix" (الكل)
+ EMAIL_PROV
- 📐 **Added session cookies authentication — confirmed 3x** — -             "User-Agent": random_ua(),
+             "User-Agent": random_ua(),   # ✅ ديناميك لكل 
- 📐 **Added JWT tokens authentication — protects against XSS and CSRF token theft — confirmed 3x** — -     provider:         str   = "mix"
+     provider:         str  = EMAIL_PROVIDER   # ✅ SSOT — مش 
- 📐 **what-changed in promptcowboy_register.py — confirmed 3x** — -     delay_between:    int   = ─────────────────────────────────────────────────────────────DELAY_B
- 📐 **what-changed in promptcowboy_register.py — confirmed 3x** — - OTP_TIMEOUT       = 15            # ثواني ماكس مستنى كود التحقق من الإيميل
+ OTP_TIMEOUT       = 3
- 📐 **Added JWT tokens authentication — protects against XSS and CSRF token theft — confirmed 3x** — - def step(num: int, total: int, msg: str):
+ def step(num_or_msg, total=None, msg=None):
-     """S
- 📐 **Added session cookies authentication — confirmed 3x** — -     # حفظ الحساب بالحقول الإلزامية
+     # ── بناء email_creds بشكل صح حسب نوع الـ client ────────
- 📐 **Added JWT tokens authentication — protects against XSS and CSRF token theft — confirmed 4x** — -             # جلب الدومينات المتاحة
+             # جلب الدومينات المتاحة — API ممكن يرجع list أو 
- 📐 **what-changed in promptcowboy_register.py — confirmed 5x** — - OTP_POLL_INTERVAL  = 5         # ثواني بين كل محاولة فحص الـ inbox
+ OTP_POLL_INTERVAL  = 4       
- 📐 **Patched security issue Domains — protects against XSS and CSRF token theft — confirmed 3x** — - # ── Domains المحجوبة من Genspark ──────────────────────
+ # ── Domains المحجوبة من Gensdef load_
- 📐 **Added session cookies authentication — protects against XSS and CSRF token theft — confirmed 3x** — - # ── Headers الأساسية للطلبات ──────────────────────────────────
+ # ── قائمة User-Agents ديناميكي
- 📐 **what-changed in promptcowboy_register.py — confirmed 12x** — -     "User-Agent":      "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like 

---
*Auto-generated by BrainSync 🧠 | 101 patterns | 2026-03-26*
