---
name: project
description: "Project for AI_PR_PR_PROVIDERS. 32 gotchas, 76 conventions."
domain: project
triggers:
  - glob: "**/*"
autoGenerated: true
enabled: true
---

# Project

Auto-compiled from **315 real patterns** in **AI_PR_PR_PROVIDERS**. This skill is auto-routed to agents when working on project files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| gotcha in brainsyncory.md | - - **gotcha in GEMINI.md** — - ❌/✅ كل الأسئلة 🔴 Critical اتجاوبت؟ + - **gotcha in GEMINI.md** — -  |
| gotcha in GEMINI.md | - ❌/✅ كل الأسئلة 🔴 Critical اتجاوبت؟ + 1. ❌/✅ كل الأسئلة 🔴 Critical اتجاوبت؟  📌 IDE AST Context |
| gotcha in GEMINI.md | - 1. ❌/✅ كل الأسئلة 🔴 Critical اتجاوبت؟ + ❌/✅ كل الأسئلة 🔴 Critical اتجاوبت؟ - 2. ❌/✅ Plan B موج |
| gotcha in GEMINI.md | - ### 📈 Improvement Tracking — في آخر كل رد فيه كود: + ### 📈 Improvement Tracking — 🟡/🔴 فقط + ل |
| gotcha in implementation_plan.md.resolved | - # PromptCowboy Register — نواقص vs template_register.py + # 🔍 تطوير نظام النقد الذاتي في GEMINI.m |
| Don't use panic for normal error handling — return | Don't use panic for normal error handling — return errors |
| Always check errors — don't use _ for error return | Always check errors — don't use _ for error return values |
| Handle exceptions specifically — not bare except: | Handle exceptions specifically — not bare except: |
| Don't use mutable default arguments — def f(items= | Don't use mutable default arguments — def f(items=[]) is a bug |
| Don't mix CommonJS (require) and ESM (import) in s | Don't mix CommonJS (require) and ESM (import) in same project |
| Don't use "any" type in TypeScript — define proper | Don't use "any" type in TypeScript — define proper types/interfaces |
| Handle Promise rejections — always .catch() or try | Handle Promise rejections — always .catch() or try/catch with await |
| Use === not == — strict equality prevents type coe | Use === not == — strict equality prevents type coercion bugs |
| Agent: follow existing project patterns — don't in | Agent: follow existing project patterns — don't introduce a different style |
| Agent: don't generate code with "any" type — defin | Agent: don't generate code with "any" type — define proper TypeScript types |
| Agent: always handle loading/error states — don't  | Agent: always handle loading/error states — don't just render data |
| Agent: don't use deprecated APIs — check library v | Agent: don't use deprecated APIs — check library version, use current API |
| Agent: check existing code before creating utility | Agent: check existing code before creating utility functions — avoid duplicates |
| Don't expose database errors to clients — map to u | Don't expose database errors to clients — map to user-friendly messages |
| Always validate data on the SERVER — client valida | Always validate data on the SERVER — client validation is for UX only |
| Don't fetch data inside render loops or hot paths | Don't fetch data inside render loops or hot paths |
| Don't swallow errors silently — empty catch blocks | Don't swallow errors silently — empty catch blocks hide bugs |
| Never store session tokens in localStorage — use h | Never store session tokens in localStorage — use httpOnly cookies |
| Use parameterized queries — never string concatena | Use parameterized queries — never string concatenation for SQL |
| Don't store passwords in plain text — use bcrypt,  | Don't store passwords in plain text — use bcrypt, argon2, or scrypt |
| Don't use innerHTML — use textContent or sanitized | Don't use innerHTML — use textContent or sanitized rendering |
| Sanitize ALL user input before database queries —  | Sanitize ALL user input before database queries — prevent SQL injection |
| Never use eval(), exec(), or Function() with user  | Never use eval(), exec(), or Function() with user input |
| Don't hardcode API keys, passwords, database URLs, | Don't hardcode API keys, passwords, database URLs, or secrets anywhere |
| Don't leave console.log, print(), debugger, or TOD | Don't leave console.log, print(), debugger, or TODO in production code |
| Don't commit .env, secrets, API keys, or credentia | Don't commit .env, secrets, API keys, or credentials — add to .gitignore |
| Never force push to main, master, or production br | Never force push to main, master, or production branches |

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **what-changed in task.md.resolved — confirmed 3x** — - - [ ] عرض الأرقام واعتماد خطة الحل مع المستخدم.
+ - [x] عرض الأرقام واعتماد خطة الحل مع المستخدم.

- 📐 **what-changed in implementation_plan.md.resolved — confirmed 4x** — - ## Verification Plan
+ ## أرقام دقيقة وحالة الملف
- عرض العدد الكلي للحسابات بالمقارنة مع العدد ال
- 📐 **what-changed in task.md — confirmed 3x** — - - [ ] عرض تقرير بالأرقام الحالية للحصول على الموافقة.
+ - [x] عرض تقرير بالأرقام الحالية للحصول عل
- 📐 **what-changed in task.md.resolved — confirmed 3x** — - - [ ] عرض تقرير بالأرقام الحالية للحصول على الموافقة.
+ - [x] عرض تقرير بالأرقام الحالية للحصول عل
- 📐 **what-changed in task.md.resolved — confirmed 3x** — - - [ ] عرض تقرير الخطة وطلب الموافقة للتنفيذ مرة أخيرة والتنبيه على تحميل النسخة.
+ - [x] عرض تقرير
- 📐 **what-changed in implementation_plan.md — confirmed 3x** — - ## Verification Plan
+ ## نتائج الفحص
- عرض عدد التكرارات والخطأ للمستخدم كخطة قابلة للمراجعة قبل 
- 📐 **what-changed in task.md — confirmed 3x** — - - [ ] عرض تقرير بالأرقام وخطة الإصلاح على المستخدم.
+ - [x] عرض تقرير بالأرقام وخطة الإصلاح على ال
- 📐 **what-changed in task.md.resolved — confirmed 5x** — - - [ ] عرض تقرير بالأرقام وخطة الإصلاح على المستخدم.
+ - [x] عرض تقرير بالأرقام وخطة الإصلاح على ال
- 📐 **what-changed in implementation_plan.md.resolved — confirmed 4x** — - ## Verification Plan
+ ## نتائج الفحص الدقيقة
- عرض كمية الحسابات الكلية والمكررة أولاً؛ وذلك كإجر
- 📐 **what-changed in task.md — confirmed 4x** — - - [ ] عرض الخطة والأرقام على المستخدم للموافقة.
+ - [x] عرض الخطة والأرقام على المستخدم للموافقة.

- 📐 **what-changed in task.md.resolved — confirmed 3x** — - - [ ] عرض الخطة والأرقام على المستخدم للموافقة.
+ - [x] عرض الخطة والأرقام على المستخدم للموافقة.

- 📐 **what-changed in implementation_plan.md — confirmed 3x** — - ## Verification Plan
+ ## نتائج الفحص (مرحلة التخطيط)
- إظهار أرقام الحسابات المكررة التي تم العثو
- 📐 **what-changed in implementation_plan.md.resolved — confirmed 3x** — - ## Verification Plan
+ ## نتائج الفحص (مرحلة التخطيط)
- إظهار أرقام الحسابات المكررة التي تم العثو
- 📐 **what-changed in task.md.resolved — confirmed 3x** — - - [ ] تنفيذ فلترة المكرر مع الحفاظ على نسخة واحدة وأرشفة الباقي (لا يوجد مكرر).
+ - [x] تنفيذ إصلا
- 📐 **Replaced auth Groq — confirmed 6x** — - # مقارنة ملفات Groq الثلاثة
+ # تنظيف حسابات Groq
- - [x] قراءة بيانات الملفات (الحجم وتاريخ التعد
- 📐 **what-changed in task.md — confirmed 4x** — - - [ ] فحص التغييرات (Diff) بين ملفي `groq_register.py` و `groq_V0/groq_register.py`.
+ - [x] فحص ا
- 📐 **what-changed in task.md.resolved — confirmed 6x** — - - [ ] فحص التغييرات (Diff) بين ملفي `groq_register.py` و `groq_V0/groq_register.py`.
+ - [x] فحص ا
- 📐 **what-changed in implementation_plan.md.resolved — confirmed 4x** — - # خطة العمل: مقارنة نسختين من سكريبت Groq
+ # خطة العمل: البحث عن الدومين الجديد
- الهدف هو مقارنة
- 📐 **Replaced auth Multi — confirmed 3x** — - # 🤠 Multi-Agent Review — PromptCowboy System
+ # 🤠 Multi-Agent Review — PromptCowboy (12 Agent)

- 📐 **what-changed in toolpix.pythonanywhere — confirmed 20x** — -   
+ 
+   {"token":"eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzUxMiJ9.eyJpYXQiOjE3NzQ0ODAyNzcsInJvbGVzIjpbIlJP
- 📐 **Added JWT tokens authentication — hardens HTTP security headers — confirmed 11x** — - "aZ8wnxV_5ro#&^
+ "aZ8wnxV_5ro#&^"
- curl 'https://toolpix.pythonanywhere.com/temp-mail' \
+ 

- 📐 **convention in toolpix.pythonanywhere** — -   
+   {"@context":"\/contexts\/Message","@id":"\/messages\/69c469edfc9f4d783f3de70f","@type":"Mes
- 📐 **what-changed in GEMINI.md — confirmed 6x** — - 5️⃣ ❌/✅ **Future Risk**
+ 5. ❌/✅ **Future Risk**

📌 IDE AST Context: Modified symbols likely in
- 📐 **decision in GEMINI.md — confirmed 4x** — - 3️⃣ هل الحل ده ممكن يسبب **technical debt** بعد فترة؟
+ 3. هل الحل ده ممكن يسبب **technical debt*
- 📐 **what-changed in GEMINI.md — confirmed 5x** — - [ما الخطوة الأفضل من وجهة نظري؟ + ليه؟ جملتان max]
+ [الخطوة الأفضل من وجهة نظري + السبب — جملتان
- 📐 **convention in GEMINI.md** — - 7. ❌/✅ Planning Score ≥ 7/10?⛔ لو Score < 7/10 → فعّل Planning Safety Rule فوراً
+ 7. ❌/✅ Plannin
- 📐 **Added API key auth authentication — ensures atomic multi-step database operat... — confirmed 6x** — - 7. ❌/✅ Planning Score ≥ 7/10?
+ 7. ❌/✅ Planning Score ≥ 7/10?⛔ لو Score < 7/10 → فعّل Planning Sa
- 📐 **what-changed in GEMINI.md — confirmed 21x** — - 5. ❌/✅ [اشياء ممكن تضر ف مستقبل المشروع و انت مقلتهاش  5]
+ 5. ❌/✅ [اشياء ممكن تضر ف مستقبل المشر
- 📐 **🟢 Edited GEMINI.md (7 changes, 3min) — confirmed 3x** — Active editing session on GEMINI.md.
7 content changes over 3 minutes.
- 📐 **what-changed in GEMINI.md — confirmed 7x** — - 1. ❌/✅ هل في dead code اتسابت؟
+ 1️⃣. ❌/✅ هل في dead code اتسابت؟

📌 IDE AST Context: Modified 
- 📐 **Added API key auth authentication — ensures atomic multi-step database operat... — confirmed 10x** — - [ما الخطوة الأفضل من وجهة نظري؟ + ليه؟ جملتان max]
+ 
- ```
+ [ما الخطوة الأفضل من وجهة نظري؟ +
- 📐 **Added API key auth authentication — ensures atomic multi-step database operat... — confirmed 5x** — - ### 📊 ملخص المستويات:
+ ### 💡 اقتراحات ذكية — إلزامي بعد كل نقد ذاتي:
- | المستوى | متى | النق
- 📐 **Added JWT tokens authentication — evolves the database schema to support new ... — confirmed 3x** — - > **⛔ قاعدة ذهبية: قبل ANY code file edit → شغّل الـ 5 agents تلقائياً وبدون طلب من المستخدم**
+ 
- 📐 **Strengthened types PromptCowboy** — - # Genspark Register — نواقص UNIVERSAL_PROVIDER_PROMPT
+ # PromptCowboy Register — نواقص vs templat
- 📐 **convention in .gitignore** — File updated (external): .gitignore

Content summary (8 lines):

AGENT.md
CLAUDE.md
.agent-mem/

# A
- 📐 **Use goroutines with proper synchronization (channels or sync)** — Use goroutines with proper synchronization (channels or sync)
- 📐 **Use context for cancellation and timeouts** — Use context for cancellation and timeouts
- 📐 **Use defer for cleanup operations (close files, unlock mutexes)** — Use defer for cleanup operations (close files, unlock mutexes)
- 📐 **Follow PEP 8 style guide** — Follow PEP 8 style guide
- 📐 **Use pathlib for file paths, not os.path string manipulation** — Use pathlib for file paths, not os.path string manipulation
- 📐 **Use virtual environments (venv, poetry, or conda)** — Use virtual environments (venv, poetry, or conda)
- 📐 **Use f-strings for string formatting, not .format() or %** — Use f-strings for string formatting, not .format() or %
- 📐 **Use context managers (with) for file and resource operations** — Use context managers (with) for file and resource operations
- 📐 **Use type hints for function arguments and return types** — Use type hints for function arguments and return types
- 📐 **Enable strict mode in tsconfig.json** — Enable strict mode in tsconfig.json
- 📐 **Prefer Array methods (map, filter, reduce) over manual loops** — Prefer Array methods (map, filter, reduce) over manual loops
- 📐 **Use template literals for string interpolation, not concatenation** — Use template literals for string interpolation, not concatenation
- 📐 **Use optional chaining (?.) and nullish coalescing (??) for safe access** — Use optional chaining (?.) and nullish coalescing (??) for safe access
- 📐 **Use const by default, let when reassignment needed, never var** — Use const by default, let when reassignmen

... [Truncated — see individual observations for full content]
