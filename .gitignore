# Git Ignore for WebDev AI Editor
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ai_runs/
.webdev_backups/
node_modules/
.env
# exception: fixture .env with FAKE keys (T-002, needed by R-204 tests)
!tests/fixtures/sample_project/.env
.venv/
venv/
ENV/
*.rar
*.zip
*.tar.gz
accounts_use_ai.json
keys.txt
active_team_backup.zip
editor_v3.rar
doc_2026-07-12_02-53-55.agents.rar
# T-028 (R-305): بيانات الجلسات لا تُتتبَّع — محادثات المستخدم ليست كودًا.
# نستهدف ملفات البيانات فقط (لا المجلد كله): sessions/__init__.py
# و sessions/store.py كود إنتاجي يبقى متتبَّعًا.
# إخراج المتتبَّع حاليًا: git rm --cached 'sessions/*.json'
# (تنظيف التاريخ نفسه = T-050)
sessions/*.json
sessions/*.jsonl
sessions/*.meta.json
sessions/*.tmp
# T-050 (R-703): مخرجات قياس التغطية — تُولَّد في كل تشغيل، لا تُتتبَّع.
# (coverage_baseline.txt متتبَّع عمدًا — هو الأرضية الرسمية للـ ratchet.)
.coverage
coverage.json
htmlcov/
# exception: TSK-623/D-3 archive of improvements/ (content preserved, tracked)
!test---results/improvements_archive_2026-07-29.tar.gz
